import React from 'react'

const Footer = ({time, msg:message, name, num1}) => {
  return (
    <div>Footer</div>
    // <div>Footer {message} Time is : {time} {name} {num1}</div>
  )
}

export default Footer